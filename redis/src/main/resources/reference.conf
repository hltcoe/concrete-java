concrete {
  redis {
    push {
      host = localhost
      port = 7379
      key = 'coe-twitter:list-queue'
      limit = 100000
    }
    
    pull {
      host = localhost
      port = 6379
      key = 'coe-twitter:list-queue'
    }

    sleep.interval = 1000
  }
}
